apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fiap-food.fullname" . }}-test-connection"
  labels:
    {{- include "fiap-food.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fiap-food.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
